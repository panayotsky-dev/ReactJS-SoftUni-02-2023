// export default function MovieList({movies}){
//     return null,
// }
export default function MovieList({movies}){
    console.log(movies)
    
    
    return (
        null
    )
}

// export default function MovieList(props)
//* function MovieList({movies}) по този начин директно деструкторираме props още при подаването на данни ,в функцията // props.movies == {movies} && props.movies.id == {movies.id}

//* можем да връщаме от компонент null 